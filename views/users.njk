{% extends 'layouts/layout.njk' %}

{% block content %}
  <h1>Users</h1>
  <ul>
    {% for user in users %}
      <li>{{user.name}} - {{user.email}} - {{user.id}}</li>
    {% endfor %}
  </ul>
{% endblock %}
